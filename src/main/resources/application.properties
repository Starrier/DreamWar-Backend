##########################   mysql   ##########################
spring.datasource.url=jdbc:mysql://localhost/dreamwar?useUnicode=true&characterEncoding=utf-8&useSSL=true
spring.datasource.username=Starrier
spring.datasource.password=starrier&1103
spring.datasource.driver-class-name=com.mysql.jdbc.Driver




##########################  mybatis   ##########################
mybatis.mapper-locations=classpath*:mybatis/*.xml
mybatis.type-aliases-package=org.starrier.dreamwar.model
mybatis.configuration.map-underscore-to-camel-case=true

##########################  druid    Configuration   ##########################
spring.datasource.type=com.alibaba.druid.pool.DruidDataSource

# 初始化大小，最小，最大
spring.datasource.initialSize=5
spring.datasource.minIdle=5
spring.datasource.maxActive=20
# 配置获取连接等待超时的时间
spring.datasource.maxWait=60000
# 配置间隔多久才进行一次检测，检测需要关闭的空闲连接，单位是毫秒
spring.datasource.timeBetweenEvictionRunsMillis=60000
# 配置一个连接在池中最小生存的时间，单位是毫秒
spring.datasource.minEvictableIdleTimeMillis=300000
# 校验SQL，Oracle配置 spring.datasource.validationQuery=SELECT 1 FROM DUAL，如果不配validationQuery项，则下面三项配置无用
spring.datasource.validationQuery=SELECT 'x'
spring.datasource.testWhileIdle=true
spring.datasource.testOnBorrow=false
spring.datasource.testOnReturn=false
        # 打开PSCache，并且指定每个连接上PSCache的大小
spring.datasource.poolPreparedStatements=true
spring.datasource.maxPoolPreparedStatementPerConnectionSize=20
# 配置监控统计拦截的filters，去掉后监控界面sql无法统计，'wall'用于防火墙
spring.datasource.filters=stat,wall,log4j
# 通过connectProperties属性来打开mergeSql功能；慢SQL记录
spring.datasource.connectionProperties=druid.stat.mergeSql=true;druid.stat.slowSqlMillis=5000
# 合并多个DruidDataSource的监控数据
spring.datasource.useGlobalDataSourceStat=true


##################   Logback ###############
logging.file=log.log
logging.level.org.starrier.dreamwar.controller=info
logging.level.org.starrier.dreamwar.util=info
spring.profiles.active=dev

logging.config=classpath:log/logback-spring.xml

########3333333333333333333#########   Redis      #######################
spring.redis.port=6379
spring.redis.host=localhost
spring.redis.database=0
spring.redis.password=
spring.redis.jedis.pool.max-active=8
spring.redis.jedis.pool.max-wait=-1ms
spring.redis.jedis.pool.min-idle=0
spring.redis.jedis.pool.max-idle=8
spring.redis.timeout=0

##############  Admin ##############
management.security.enabled=false
endpoints.health.sensitive=false
spring.jackson.serialization.indent_output=true
server.display-name=springbootserver
server.context-path=/
server.servlet-path=/
# admin



################## ElasticSearch ######################




##################  JPA #############
spring.jpa.hibernate.naming.strategy=org.hibernate.cfg.ImprovedNamingStrategy
spring.jpa.show-sql=true
spring.jpa.database: MYSQL
